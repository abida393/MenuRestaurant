<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="24dp"
    app:cardBackgroundColor="#FFFFFF"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Image Area -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="180dp">

            <ImageView
                android:id="@+id/iv_dish_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                tools:src="@android:color/darker_gray" />

            <!-- Favorite Star Icon -->
            <ImageView
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="top|end"
                android:layout_marginTop="12dp"
                android:layout_marginEnd="12dp"
                android:background="@drawable/bg_icon_circle_white"
                android:padding="8dp"
                android:src="@android:drawable/star_on"
                android:tint="#A02020" />
        </FrameLayout>

        <!-- Content Area -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Category / Badge -->
            <TextView
                android:id="@+id/tv_category"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:letterSpacing="0.05"
                android:paddingHorizontal="6dp"
                android:paddingVertical="2dp"
                android:textColor="#5A6D88"
                android:textSize="9sp"
                android:textStyle="bold"
                tools:background="@drawable/bg_badge_light_blue"
                tools:text="CHEF'S SPECIAL" />

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:fontFamily="serif"
                android:lineSpacingMultiplier="1.1"
                android:textColor="#1C1C1A"
                android:textSize="20sp"
                tools:text="Black Truffle\nTagliatelle" />

            <TextView
                android:id="@+id/tv_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:lineSpacingMultiplier="1.3"
                android:textColor="#555555"
                android:textSize="11sp"
                tools:text="Hand-rolled pasta tossed in a rich butter sauce, finished with aged Parmigiano-Reggiano and fresh..." />

            <!-- Price & Actions Row -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp">

                <TextView
                    android:id="@+id/tv_price"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerVertical="true"
                    android:fontFamily="serif"
                    android:textColor="#A02020"
                    android:textSize="18sp"
                    tools:text="$34.00" />

                <!-- Action Button Container (Will be configured dynamically in Java) -->
                <FrameLayout
                    android:id="@+id/fl_action_container"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true">

                    <TextView
                        android:id="@+id/tv_action_button"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:letterSpacing="0.05"
                        android:paddingHorizontal="16dp"
                        android:paddingVertical="6dp"
                        android:textSize="10sp"
                        android:textStyle="bold"
                        tools:background="@drawable/bg_btn_red_small"
                        tools:text="RESERVE"
                        tools:textColor="#FFFFFF" />
                </FrameLayout>
            </RelativeLayout>
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
